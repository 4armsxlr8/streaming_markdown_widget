name: streaming_markdown_widget
description: "A widget that reveals streaming Markdown one character at a time while keeping it formatted, with a collapsible thinking frame."
version: 0.1.0
repository: https://github.com/4armsxlr8/streaming_markdown_widget
issue_tracker: https://github.com/4armsxlr8/streaming_markdown_widget/issues

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  markdown: ^7.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
